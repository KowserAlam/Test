name: jobxprss_company
description: A new Flutter application for Job EXpress.

# The following line prevents the package from being accidentally published to
# pub.dev using `pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.27+32

environment:
  sdk: ">=2.8.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.0
  provider: ^4.3.2+2
  font_awesome_flutter: ^8.8.1
  flutter_feather_icons: ^1.0.3
  rxdart: ^0.24.1
  http: ^0.12.2
  after_layout: ^1.0.7+2
  shared_preferences: ^0.5.12
  intl: ^0.16.1
  page_view_indicators: ^1.4.0
  uuid: ^2.2.2
  image_cropper: ^1.3.1
  image_picker: ^0.6.7+11
  package_info: ^0.4.3
  dartz: ^0.9.2
  google_fonts: ^1.1.0
  bot_toast: ^3.0.4
  url_launcher: ^5.7.2
  cached_network_image: ^2.3.2+1
  flutter_image_compress: ^0.7.0
  equatable: ^1.2.5
#  connectivity: ^0.4.9+3
  logger: ^0.9.4
  get_it: ^5.0.1
  flutter_typeahead: ^1.8.8
  google_maps_flutter: ^1.0.2
  webview_flutter: ^1.0.0
  charts_flutter: ^0.9.0
  shimmer: ^1.1.1
  percent_indicator: ^2.1.7+4
  jwt_decode: ^0.1.0
  dropdown_search: ^0.4.4
  flutter_cache: ^0.0.6
  permission_handler: ^5.0.1+1
  geolocator: 5.3.2+2
  flutter_widget_from_html_core: ^0.5.0+7
  quill_delta: 1.1.3
  notustohtml: ^0.0.7
  zefyr: ^0.12.0
  flutter_linkify: ^3.1.3
  social_share: ^2.0.5
  get: ^3.12.1
  socket_io_client: ^0.9.11
#  velocity_x: ^0.4.1


dependency_overrides:
  platform: 3.0.0-nullsafety.1


#  zefyr:
#    git:
#      url: https://github.com/memspace/zefyr.git
#      path: packages/zefyr
#      commit: ea6c29a7cfce3091b07fd0a37f081bb0f16d2361


dev_dependencies:
  flutter_test:
    sdk: flutter
  test:
  flutter_driver:
    sdk: flutter
  flutter_launcher_icons: ^0.8.1
#    flutter_flavorizr: ^1.0.7


flutter_icons:
  android: true
  ios: true
  image_path: "assets/images/logo_sq.png"

flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/images/
    - assets/certification/

